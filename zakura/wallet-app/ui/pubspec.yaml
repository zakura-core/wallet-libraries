name: zakura_ui
description: Overridable widgets and theme tokens for a Zakura wallet.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  zakura_client:
    path: ../client

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

name: zakura_state
description: Riverpod providers over the Zakura wallet client.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.0
  zakura_client:
    path: ../client

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
